<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="memberMapper">

	<resultMap id="adminMemberResultSet" type="adminMember">
		<id column="user_id" property="userId"></id>
		<result column="user_type" property="userType" />
		<result column="user_pwd" property="userPwd" />
		<result column="user_nickname" property="userNickname" />
		<result column="user_email" property="userEmail" />
		<result column="user_phone" property="userPhone" />
		<result column="user_profile" property="userProfile" />
		<result column="user_profile_c" property="userProfileC" />
		<result column="profile_path" property="profilePath" />
		<result column="user_status" property="userStatus" />
		<result column="enroll_date" property="enrollDate" />
		<result column="modify_date" property="modifyDate" />
		<result column="withdrawal_date" property="withdrawalDate" />
		<result column="user_memo" property="userMemo" />
	</resultMap>
	
	<!-- 총 회원수 조회 -->
	<select id="selectListCount" resultType="_int">
		select
		       count(*)
		  from member
	</select>
	
	<!-- 모든 회원리스트 조회 -->
	<select id="selectList" resultMap="adminMemberResultSet">
		select
		       user_type
		     , user_id
		     , user_nickname
		     , user_email
		     , user_phone
		     , enroll_date
		     , modify_date
		     , user_status
		  from member
		 order
		    by user_status desc
	</select>
	
	<!-- 회원유형 검색결과조회 -->
	<select id="selectSearchUserType" parameterType="String" resultMap="adminMemberResultSet">
		select
		       user_type
		     , user_id
		     , user_nickname
		     , user_email
		     , user_phone
		     , enroll_date
		     , modify_date
		     , user_status
		  from member
		 where user_type like #{searchText}||'%'
		 order by decode(user_status, 'Y', 1), enroll_date desc
	</select>
	
	<!-- 회원아이디 검색결과조회 -->
	<select id="selectSearchUserId" parameterType="String" resultMap="adminMemberResultSet">
		select
		       user_type
		     , user_id
		     , user_nickname
		     , user_email
		     , user_phone
		     , enroll_date
		     , modify_date
		     , user_status
		  from member
		 where user_type like #{searchText}||'%'
		 order by decode(user_status, 'Y', 1), enroll_date desc
	</select>
	
	<!-- 선택한 회원상태('N'인경우)로 우선정렬조회 -->
	<select id="selectStatusSearchList" parameterType="String" resultMap="adminMemberResultSet">
		select
		       user_type
		     , user_id
		     , user_nickname
		     , user_email
		     , user_phone
		     , enroll_date
		     , modify_date
		     , user_status
		  from member
		 order by decode(user_status, #{userStatus}, 1), enroll_date desc
	</select>
</mapper>